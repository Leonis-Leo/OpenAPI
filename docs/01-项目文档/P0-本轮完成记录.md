# P0 本轮完成记录

日期：2026-08-08

本轮已完成并验证：

- SecretKey 重置要求输入当前登录密码，避免仅靠前端确认。
- Redis nonce 防重放改为故障拒绝，Redis 不可用时返回 503。
- 登录失败保护：同一账号 15 分钟窗口最多 5 次失败，触发 15 分钟锁定。
- JWT 注销：退出登录后 token 写入 Redis 黑名单，后续请求返回 401。
- CSRF：登录签发 `openapi_csrf` Cookie，所有 `/v1/**` 写请求要求 `X-CSRF-Token` 双提交校验。
- 数据库、Redis、RabbitMQ、JWT 和 Cookie 配置支持环境变量覆盖。
- 增加 `audit_log` 表、MyBatis 映射和 `/v1/**` 操作审计拦截器，敏感参数不写入日志。
- 一键启动脚本优先使用 JDK 17，修复 E 盘旧 Java 版本导致后端启动失败的问题。

验证结果：后端 Maven 打包通过；前端 `npm.cmd run build` 通过；登录、应用列表、CSRF、注销后 401、审计记录均已联调通过。

数据库增量迁移：`db/migrations/2026-08-08-audit-log.sql`。

## 管理后台视觉优化

- 概览和统计页的图表高度收紧；无数据或仅有一天数据时显示明确空态与汇总指标。
- 调用排行增加相对调用量进度条，减少卡片空白并强化数据对比。
- 应用、接口、日志等列表页统一批量操作栏、按钮层级、表格行间距和分页样式。
- AccessKey、SecretKey、接口路径使用更适合技术字段的等宽字体；SecretKey 增加行内复制入口和显隐反馈。
- 统一空状态、表格 hover、标签圆角、弹窗内容和响应式间距。

验证：`npm.cmd run build` 通过；仅保留既有 Vite 依赖注释和 bundle 体积提示。

## P1 第一项：在线调试增强

- 在线调试支持 POST 表单参数与 JSON Body 两种模式。
- 实际请求的 `Content-Type`、请求体和复制 Curl 保持一致。
- 按接口保存最近 10 条调试请求历史，支持回填和清空。

验证：前端 `npm.cmd run build` 通过；`git diff --check` 通过。

## P1 第二项：应用与接口服务端分页

- 新增 `/v1/app/page`，支持按应用名称、AccessKey、状态和页码查询当前用户应用。
- 新增 `/v1/interface/page`，支持管理员/普通用户权限范围内的接口分页、名称/路径搜索和上线状态筛选。
- 应用管理和接口管理页面切换为服务端分页；订阅状态筛选继续基于订阅映射做本地二次筛选，后续再下沉为联表查询。

验证：`mvn.cmd -q -pl openapi-backend -am test`、前端 `npm.cmd run build`、`git diff --check` 通过。

## P1 第三项：用户分页与安全导出

- 新增 `/v1/user/page`，管理员用户列表支持服务端分页和账号/昵称搜索。
- 用户管理页面新增 CSV 导出，导出字段不包含密码等敏感信息，并提示当前导出当前页数据。

验证：后端 Maven 测试、前端构建和 `git diff --check` 均通过。

## P1 第四项：订阅分页与导出

- 新增 `/v1/interface/subscribes/page`，管理员可按状态分页查询全部/待审批订阅。
- 新增 `/v1/interface/my-subscribes/page`，用户可分页查询自己的订阅。
- 订阅审批页面三个视图切换为服务端分页，并增加当前页 CSV 导出。
- 导出字段限定为接口、路径、应用、申请人、状态和申请时间。

验证：后端 Maven 测试、前端构建和 `git diff --check` 均通过。

## P1 第五项：修复订阅审批首次进入报错

- 修复 `/v1/interface/my-subscribes/page` 使用 MyBatis-Plus 链式查询对象参与分页导致的 500。
- 改为标准 `LambdaQueryWrapper` 分页查询，避免首次进入订阅审批页面时并发请求连续提示“系统内部错误”。
- 重启后端并验证管理员登录后的全部订阅分页接口均返回 200。

## P1 第六项：网络异常统一兜底

- 新增独立的“网络异常”页面，避免请求无响应时在当前页面重复弹出多个错误提示。
- 仅对断网、请求超时、网关无响应等无响应场景跳转；接口返回的权限、业务和服务端错误仍保留当前页提示。
- 增加“重新连接”和“返回首页”操作，并允许未登录用户访问异常页。
