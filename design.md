# OpenAPI 开放平台 · 设计规范

> 本文件是项目唯一的设计规范来源。**新建或修改任何页面/组件前，必须先通读本文件**，并严格遵循其中的色值、字体、间距、组件样式与布局规则，保证全站风格统一。
>
> 技术栈：Vue 3 + TypeScript + Element Plus 2.x，样式以 `openapi-web/src/styles/base.css` 的全局变量与覆盖为基准。

---

## 1. 配色

### 1.1 语义色（全局变量定义于 `base.css :root`，暗色模式见 `.dark`）

| 用途 | 变量 | 色值 |
|------|------|------|
| 主色（Primary） | `--app-primary` | `#2563eb` |
| 主色 hover / active | - | `#1d4ed8` / `#1e40af` |
| 主色浅底（选中/强调） | - | `#eff6ff` |
| 主色浅边 | - | `#bfdbfe` |
| 成功 | - | 文字 `#027a48`，边框 `#a6f4c5`，浅底 `#ecfdf3` |
| 警告 | - | 文字 `#b54708`，边框 `#fedf89`，浅底 `#fffaeb` |
| 危险 | - | 文字 `#b42318`，边框 `#fda29b`，浅底 `#fef3f2` |
| 中性信息 | - | 文字 `#475467`，边框 `#d0d5dd` |

### 1.2 页面与表面

| 用途 | 变量 | 色值 |
|------|------|------|
| 页面背景 | `--app-bg` | `#f7f9fc` |
| 卡片/表面 | `--app-surface` | `#ffffff` |
| 边框 | `--app-border` | `#e7edf4` |
| 按钮描边 | - | `#d0d5dd` |
| 表格边框 | - | `#edf1f6` |

### 1.3 文字

| 用途 | 变量 | 色值 |
|------|------|------|
| 主文字 | `--app-text` | `#172033` |
| 次要文字 | `--app-muted` | `#64748b` |
| 按钮正文 | - | `#344054` |
| 禁用文字 | - | `#98a2b3` |
| 表头文字 | - | `#64748b` |

### 1.4 导航

| 用途 | 色值 |
|------|------|
| 侧边栏背景 | `#172b4d`（暗色 `#0b1324`） |
| 菜单未选中 | 文字 `#a8b5ca`，hover 底 `rgba(148,163,184,.12)` |
| 菜单选中 | 底 `#dbeafe`，文字/图标 `#1d4ed8`，左侧 3px 竖条 `#2563eb` |
| 运行状态点 | `#34d399` |

### 1.5 规则
- 每个视图**只有一个实心主色按钮**（主操作）；其余操作用描边或文字按钮。
- 语义不只靠颜色传达，危险/成功操作需同时有文字与图标/标签。
- 文本与背景对比度满足 WCAG AA（正文 ≥ 4.5:1）。

---

## 2. 字体

字体族：`Inter, "PingFang SC", "Microsoft YaHei", sans-serif`；等技术字段用 `"JetBrains Mono", Consolas, monospace`。

| 场景 | 字号 | 字重 | 行高 | 备注 |
|------|------|------|------|------|
| 页面大标题（列表页 h2） | 24px | 700 | 1.2 | `letter-spacing: -.02em` |
| 页面标题（接口管理等） | 22px | 700 | 1.2 | 同上 |
| 页面副标题 | 13px | 400 | 1.7 | `--app-muted` |
| 卡片标题 | 15-16px | 700 | 1.3 | - |
| 正文/表体 | 14px | 400 | 1.5-1.7 | - |
| 按钮 | 13px（small 12px） | 500-600 | - | - |
| 表头 | 12px | 600 | 20px | `#64748b` |
| 表体技术字段（AK/SK/路径） | 12px | 400 | 1.6 | 等宽字体 |
| 统计大数字 | 26-30px | 600-700 | 1.2 | - |
| 小标签/提示 | 11-12px | 400-500 | - | `--app-muted` |

规则：不跳级使用标题；标题用 heading 语义元素（h1/h2），正文不要用标题样式。

---

## 3. 间距

间距刻度（4px 基准）：`4 / 8 / 12 / 16 / 20 / 24`，禁止随意取 13px、23px 等非刻度值。

| 场景 | 值 |
|------|-----|
| 页面主区外边距 | `28px 32px 40px`（窄屏 `20px`） |
| 模块/卡片间距 | `16px` |
| 筛选卡、按钮卡内边距 | `12px 14px`（按钮卡 `10px 14px`） |
| 筛选卡/按钮卡与下方间距 | `12px` |
| 按钮组内间距 | `8px` |
| 标题区与筛选区间距 | `14px` |
| 表格行高 | `40px`（`td` 上下 `6px`，`th` 上下 `8px`） |
| 控件高度 | 常规 32px，主按钮 36px，大按钮 42px |
| 圆角 | 卡片 12px，按钮 8px，小按钮 7px，标签 6px，弹窗 14px |

### 内容居中
- 行内容垂直居中：`display: flex; align-items: center`。
- 标题区：标题左对齐，操作按钮右对齐（`justify-content: space-between`）。
- 筛选区/按钮区：控件左组 + 操作右组（`space-between`），控件之间 `gap: 8px`。
- 分页固定在最右侧，不被换行挤压（多按钮时按钮区分两行，分页行独立）。

---

## 4. 组件样式

### 4.1 按钮
| 类型 | 样式 |
|------|------|
| 主操作（Primary） | 实心 `#2563eb`、白字、圆角 8px、阴影 `0 1px 2px rgba(16,24,40,.06)`；hover `#1d4ed8` |
| 次级（Secondary） | 白底、`1px solid #d0d5dd`、文字 `#344054`、阴影 `0 1px 2px rgba(16,24,40,.04)`；hover 底 `#f9fafb`、边 `#98a2b3` |
| 语义按钮 | 描边弱化：成功/警告/危险分别用 1.1 节色值（文字+边框+浅底 hover） |
| 文字按钮 | 无边框无底，hover 浅蓝底 `#eff6ff`（用于“分组标签”等轻操作） |
| 禁用 | 底 `#f2f4f7`、文字 `#98a2b3`、边框 `#eaecf0`、`opacity: 1`、`cursor: not-allowed` |
| 焦点 | `outline: 2px solid rgba(37,99,235,.35)`，`outline-offset: 1px` |

规则：
- 一屏一个实心主按钮；批量/次级操作统一描边。
- 互斥操作合并为自适应按钮（如 订阅/取消订阅、上线/下线、启用/禁用、角色切换）：全部 A 显示 A、全部 B 显示 B、混杂禁用。
- 低频或危险操作收进“更多 ▾”下拉（重置密钥、重置密码等）；删除用红字描边放右侧。

### 4.2 卡片
| 类型 | 样式 |
|------|------|
| 通用卡片（筛选/按钮/表格容器） | 白底、`1px solid var(--app-border)`、圆角 12px、阴影 `0 4px 18px rgba(30,64,175,.04)` |
| 筛选卡 | 白底、边框、圆角 12px、阴影 `0 1px 3px rgba(16,24,40,.04)`、内边距 `12px 14px` |

### 4.3 导航栏
- 侧边栏深色 `#172b4d`，宽 240px（折叠 76px）。
- 菜单分组标签：11px、字距 `.08em`、`#71809a`。
- 菜单项高 44px、圆角 9px、外边距 `3px 12px`；选中态见 1.4。
- “规划中”占位菜单带小徽标（10px，圆角 6px）。
- 顶栏高 72px，白底、底边框；右侧图标按钮 34×34 圆角 8px。

### 4.4 表格
- 紧凑行：行高 40px，`td` 上下 6px、`th` 上下 8px。
- 边框 `1px solid #edf1f6`，圆角 10px，表头底 `#fbfcfe`。
- 行 hover `#f8fbff`，当前行 `#eff6ff`。
- 技术字段（AccessKey/SecretKey/路径）等宽字体 12px。

### 4.5 筛选控件
- 筛选 Tab/分段控件：灰底轨道 `#f2f4f7`、内边距 2px、圆角 9px；选中段白底浮起 `0 1px 2px rgba(16,24,40,.08)`。
- 输入框/下拉：`box-shadow: 0 0 0 1px var(--app-border) inset`，hover `#93c5fd`。
- 日期选择：优先“预设下拉 + 自定义”模式（近 7/30/90 天），默认 130px，自定义才展开日期范围。

---

## 5. 组件布局（列表页标准结构）

每个列表页自上而下固定为五层：

```
① 标题区：标题（+副标题）左对齐，不放任何按钮
② 筛选区：CollapsibleFilter 卡片
     - 常用条件内联左侧（搜索框/下拉），占位符代替标签
     - 次要条件放「更多筛选 ▾ / 收起 ▴」
     - 右侧固定 重置 / 查询
③ 按钮区：action-bar 卡片
     - 第一行：页面级操作（导出/新增等）左组 + 分页右组（分页始终固定右上，不换行）
     - 第二行：仅在选中数据时出现——批量操作 + 已选 N 项胶囊 + 删除（红字）
     - 未选中数据时隐藏批量行，页面只保留清爽的一行
     - 低频危险操作进「更多 ▾」
④ 表格区：紧凑表格卡片
⑤ 底部：无分页（已并入按钮区）
```

### 特殊情况
- **接口管理**：左侧为分组树（可拖拽缩放 160-360px，可收起），右侧内容区套用上述标准。
- **调用明细/API 日志**：按钮区与表格区是两张独立卡片，中间有间距。
- **弹窗/抽屉**：圆角 14px，头部内边距 `22px 24px 16px`，正文 `8px 24px 24px`。
- **空/加载/错误态**：表格空态用 `el-empty` 描述文案；加载用 `v-loading` 或骨架；模块错误给“重试”按钮。

---

## 6. 组件体验流程

### 6.1 列表页浏览
1. 进入页面 → 标题 + 副标题，无操作按钮。
2. 常用筛选默认可见；点“更多筛选”展开次要条件；点“查询”生效、“重置”清空全部（含 URL 带来的过滤）。
3. 按钮区按钮在未选中数据时禁用（浅灰），选中后点亮。

### 6.2 选择与批量操作
1. 勾选表格行（支持全选）。
2. 按钮区显示“已选 N 项”胶囊；批量按钮按选中态自适应文案（如“启用/禁用”）。
3. 危险操作（删除、重置密钥等）需二次确认（`ElMessageBox.confirm`）。
4. 操作完成 → `ElMessage` 汇总成功/失败条数 → 刷新列表并清空选择。

### 6.3 筛选 → 明细联动
- 统计页/调用明细点击行 → 跳转 API 日志并带 `date/appId/interfaceId` 查询参数；日志页自动过滤并显示可清除的过滤标签。

### 6.4 空态与异常
- 列表无数据：表格内 `el-empty` 提示。
- 网络异常：全局跳转 `/network-error`（仅一次），普通错误 toast 去重；后台轮询静默。

### 6.5 自适应按钮语义
| 操作组 | 规则 |
|--------|------|
| 订阅/取消订阅 | 全部可订阅→“订阅”；全部已订阅→“取消订阅”；混杂禁用 |
| 上线/下线 | 全部下线→“上线”；全部上线→“下线”；混杂禁用 |
| 启用/禁用 | 全部禁用→“启用”；全部启用→“禁用”；混杂禁用 |
| 设为管理员/普通用户 | 按选中用户角色自适应文案与目标 |

---

## 7. 落地清单（新页面必须对照）

- [ ] 标题区只放标题，按钮全部在按钮区
- [ ] 筛选用 `CollapsibleFilter`（卡片底色 + 常用/更多 + 重置/查询）
- [ ] 按钮区用 `action-bar` + `bar-left/bar-right` + `bar-pagination`
- [ ] 互斥操作合并为自适应按钮；低频进“更多”
- [ ] 表格紧凑（40px 行高），分页在按钮区右侧
- [ ] 色值/字体/间距全部取自本规范，不使用非刻度值
- [ ] 空态、加载、错误、焦点、键盘可达性齐全
